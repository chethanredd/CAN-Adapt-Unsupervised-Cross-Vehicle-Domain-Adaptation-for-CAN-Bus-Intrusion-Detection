# Data Directory

This public copy does not bundle raw automotive CAN datasets.

Place legally obtained datasets here when running locally:

```text
data/
  can-v1.5/
  road/
  mirgu/
```

The offline demo generates synthetic CAN-like traffic at runtime and does not require external datasets.

